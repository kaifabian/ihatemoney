Hallo,

jemand, der die E-Mail-Adresse {{ g.project.contact_email }} verwendet hat Sie eingeladen, gemeinsame Rechnungen im Projekt "{{ g.project.name }}" zu verwalten.

Das ist sehr einfach: Sie müssen nur angeben, wieviel Sie für wen und wofür bezahlt haben, und wir kümmern uns um den Rest.

Sie können sich über diesen Link einloggen: <{{ url_for(".authenticate", _external=True, token=g.project.generate_token()) }}>.
Sobald Sie eingeloggt sind, können Sie den folgenden, einfacheren Link verwenden: <{{ url_for(".list_bills", _external=True) }}>

Sollten Ihre Cookies gelöscht werden (oder sie sich ausloggen), verwenden Sie bitte den ersten Link, um sich wieder einzuloggen.

Viel Spaß!
Ein Haufen (bärtiger) komischer Typen
